name: tale_drawer_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tale_drawer:
    path: ../../
  tools_tkmonkey:
    git:
      url: git://github.com/TKMonkey/tools_tkmonkey_flutter.git
      ref: dev

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
